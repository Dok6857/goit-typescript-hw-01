function add(num1: number, num2: number) {
    return num1 + num2;
  }
  
  add(1, 1);
  
  let fullName: string = '';
  let age = 12;

//   const something: boolean[] = [3, 'something', true]

type user = {}